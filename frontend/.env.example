# Utilisé par le frontend pour appeler l'API.
# - En DEV (avec Vite), laisse VITE_API_BASE_URL vide pour utiliser le proxy /api.
# - En PROD (Dockerfile build), définir VITE_API_BASE_URL pour pointer vers l'API publique.
VITE_API_BASE_URL=http://localhost:4000

# Cible du proxy Vite en développement (utile en Docker Compose dev)
VITE_DEV_PROXY_TARGET=http://localhost:4000
